apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-api-chart.fullname" . }}
  labels:
    {{- include "node-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-api-chart.selectorLabels" . | nindent 4 }}
